<table class="table table-striped table-responsive table-hover">
    <thead>
    <tr>
        <th>Key</th>
        <th>Type</th>
        <th class="text-right">TTL in ms</th>
    </tr>
    </thead>
    <tbody>
    {% for keyInfo in keyInfoObjects %}
        <tr>
            <td>
                <a href="#keyInfoModal" data-key="{{ keyInfo.getName }}" data-toggle="modal" role="button">
                    {{ keyInfo.getName }}
                </a>
            </td>
            <td>
                {{ keyInfo.getType }}
            </td>
            <td class="text-right">
                {% if keyInfo.getTimeToLive == -1 %}
                    ∞
                {% elseif keyInfo.getTimeToLive == -2 %}
                    not existing
                {% else %}
                    {{ keyInfo.getTimeToLive }}
                {% endif %}
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="3">
                <h3 class="text-center">
                    No data found.
                </h3>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{# Modal: Key Info #}

<div class="modal fade" id="keyInfoModal" tabindex="-1" role="dialog" aria-labelledby="keyInfoModalLabel"
     data-load-url="{{ appConfig.getBaseUrl }}/ajax.php"
     data-database="{{ database }}"
     data-server="{{ serverIndex }}">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title" id="keyInfoModalLabel"></h4>
            </div>
            <div class="modal-body">

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>